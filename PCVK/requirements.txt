# --- Server & API Framework (WAJIB DITAMBAH) ---
fastapi
uvicorn
python-multipart    # Wajib untuk handle upload file gambar

# --- Core ML & Logic (TETAP DIPAKAI) ---
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
lightgbm>=3.3.0
joblib>=1.1.0

# --- Image Processing ---
# Gunakan versi 'headless' agar lebih ringan & stabil di server cloud (Render/Linux)
opencv-python-headless>=4.5.0
pillow>=9.0.0
scikit-image>=0.19.0

# --- Deep Learning (Segmentasi U2Net) ---
onnxruntime>=1.12.0
